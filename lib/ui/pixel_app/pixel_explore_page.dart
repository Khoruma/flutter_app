import 'package:flutter/material.dart';

class PixelExplorePage extends StatelessWidget {
  const PixelExplorePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('explore page'),
    );
  }
}
